[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "common_math_sense"
version = "1.1.0"
description = "A simple, human-friendly mathematics library for Python"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "sympy>=1.14"
]